tool_decision_matrix:
  github:
    use_when:
      - leggere repository
      - verificare file
      - creare patch
  google_drive:
    use_when:
      - analizzare documenti utente
      - leggere cartelle progetto
  python:
    use_when:
      - generare zip
      - validare yaml/json
      - creare manifest
  web:
    use_when:
      - dati aggiornati
      - benchmark esterni
      - informazioni temporali

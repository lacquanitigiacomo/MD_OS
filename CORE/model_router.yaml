model_router:
  chatgpt:
    best_for:
      - reasoning_deep
      - code
      - structured_outputs
      - product_strategy
  gemini:
    best_for:
      - long_context
      - multimodal_large_context
  claude:
    best_for:
      - writing_refinement
      - synthesis
      - tone
  local_ai:
    best_for:
      - private_docs
      - offline
      - batch_extraction

selection_rule:
  - choose_best_model_for_phase
  - prefer_current_runtime_when_external_model_unavailable
  - never_assume_external_model_access
